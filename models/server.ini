[model]
name = Server
label = {{ this.title }}

[fields.sort_key]
label = Sort order
type = sort_key

[fields.title]
label = Title
type = string

[fields.address]
label = Address
type = string

[fields.body]
label = Body
type = markdown
