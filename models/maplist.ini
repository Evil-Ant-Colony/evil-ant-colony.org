[model]
name = Maplist
label = {{ this.title }}

[fields.sort_key]
label = Sort order
type = sort_key

[children]
order_by = sort_key
model = mappage

[fields.title]
label = Title
type = string

[fields.body]
label = Body
type = markdown

[pagination]
enabled = yes
per_page = 10
