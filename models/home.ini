[model]
name = Home
label = {{ this.title }}

[fields.sort_key]
label = Sort order
type = sort_key

[children]
order_by = sort_key

[fields.title]
label = Title
type = string

[fields.body]
label = Body
type = markdown
